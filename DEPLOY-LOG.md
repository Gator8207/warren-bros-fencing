# Deploy log

Root-level file (not served) — used to trigger Cloudflare Git auto-deploy builds.

- 2026-06-05 — Connected GitHub auto-deploy to the `warrenbrosfencing` Worker. First Git-triggered build (publishes the bold homepage from ./site).
